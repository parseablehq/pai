---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: parseableconfigs.observability.parseable.com
spec:
  group: observability.parseable.com
  names:
    kind: ParseableConfig
    listKind: ParseableConfigList
    plural: parseableconfigs
    singular: parseableconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ParseableConfig is the Schema for the parseableconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ParseableConfigSpec defines the desired state of ParseableConfig
            properties:
              instrumentation:
                description: Instrumentation defines auto-instrumentation settings
                properties:
                  languageDetection:
                    description: LanguageDetection specifies how to detect application
                      languages
                    enum:
                    - auto
                    - manual
                    type: string
                  languages:
                    description: Languages specifies which programming languages to
                      instrument
                    items:
                      type: string
                    type: array
                  mode:
                    description: Mode specifies the instrumentation mode
                    enum:
                    - auto
                    - manual
                    - hybrid
                    type: string
                type: object
              kubernetesEvents:
                description: KubernetesEvents defines Kubernetes event collection
                  settings
                properties:
                  enabled:
                    description: Enabled specifies whether Kubernetes event collection
                      is enabled
                    type: boolean
                  eventTypes:
                    description: EventTypes specifies which event types to collect
                    items:
                      type: string
                    type: array
                type: object
              logs:
                description: Logs defines logging configuration
                properties:
                  collector:
                    description: Collector specifies the log collector to use
                    type: string
                  enabled:
                    description: Enabled specifies whether log collection is enabled
                    type: boolean
                  enrichment:
                    description: Enrichment defines log enrichment options
                    properties:
                      kubernetesMetadata:
                        description: KubernetesMetadata enables enriching logs with
                          Kubernetes metadata
                        type: boolean
                    type: object
                type: object
              namespaceSelector:
                description: NamespaceSelector defines which namespaces to include
                  or exclude from instrumentation
                properties:
                  mode:
                    description: Mode specifies whether to include or exclude the
                      listed namespaces
                    enum:
                    - include
                    - exclude
                    type: string
                  namespaces:
                    description: Namespaces is the list of namespace names
                    items:
                      type: string
                    type: array
                type: object
              nodeMetrics:
                description: NodeMetrics defines node-level metrics collection settings
                properties:
                  collectors:
                    description: Collectors specifies which metric collectors to enable
                    items:
                      type: string
                    type: array
                  enabled:
                    description: Enabled specifies whether node metrics collection
                      is enabled
                    type: boolean
                  scrapeInterval:
                    description: ScrapeInterval is the interval at which to scrape
                      metrics
                    type: string
                type: object
              resources:
                description: Resources defines resource configurations for operator-managed
                  workloads
                properties:
                  daemonSet:
                    description: DaemonSet defines resources for DaemonSet workloads
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Limits describes the maximum amount of compute
                          resources allowed
                        type: object
                    type: object
                type: object
              target:
                description: Target defines the Parseable endpoint configuration
                properties:
                  credentialsSecret:
                    description: CredentialsSecret references the secret containing
                      authentication credentials
                    properties:
                      name:
                        description: Name is the name of the secret
                        type: string
                      namespace:
                        description: Namespace is the namespace of the secret
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  endpoint:
                    description: Endpoint is the Parseable API endpoint URL for ingestion
                    type: string
                  streams:
                    description: Streams defines the stream names for different telemetry
                      types
                    properties:
                      logs:
                        description: Logs is the stream name for log data
                        type: string
                      metrics:
                        description: Metrics is the stream name for metric data
                        type: string
                      traces:
                        description: Traces is the stream name for trace data
                        type: string
                    type: object
                required:
                - credentialsSecret
                - endpoint
                type: object
            required:
            - target
            type: object
          status:
            description: ParseableConfigStatus defines the observed state of ParseableConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
